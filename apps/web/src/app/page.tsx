export default function Home() {
  return (
    <main style={{ padding: 24 }}>
      <h1>TAN Web</h1>
      <p>Next.js + Prisma + NextAuth; mobile API endpoints are under <code>/api/mobile/*</code>.</p>
      <a href="/signin">Sign in</a>
    </main>
  );
}